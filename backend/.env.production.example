# 本番環境用の環境変数テンプレート
# Renderの環境変数設定でこれらの値を設定してください

NODE_ENV=production
PORT=5001

# データベース接続（RenderのPostgreSQLから自動取得）
DATABASE_URL=<RenderのPostgreSQL Internal Database URLをここに設定>

# JWT設定（強力なランダム文字列に変更してください）
JWT_SECRET=<強力なランダム文字列を生成してください>

# フロントエンドURL（RenderのStatic SiteのURLまたはカスタムドメイン）
FRONTEND_URL=<https://your-frontend.onrender.com>

# LINE Notify設定（オプション）
LINE_NOTIFY_TOKEN=<LINE Notifyトークン>
